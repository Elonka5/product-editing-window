import { MobileTrancatePipe } from './mobile-trancate-pipe';

describe('MobileTrancatePipe', () => {
  it('create an instance', () => {
    const pipe = new MobileTrancatePipe();
    expect(pipe).toBeTruthy();
  });
});
